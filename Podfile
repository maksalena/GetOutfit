# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'GetOutfit' do
  use_frameworks!

  # Pods for GetOutfit
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  
end
